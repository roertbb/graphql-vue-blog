query UpdateComment($user_id: ID!, $node_id: ID!) {
  SomeCommentExists(filter: { id: $node_id, author: { id: $user_id } })
}

query DeleteComment($user_id: ID!, $node_id: ID!) {
  SomeUserExists(filter: { id: $user_id })
}
